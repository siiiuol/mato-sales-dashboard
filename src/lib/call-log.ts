/**
 * @deprecated Importeer uit `./contact-log`. Blijft als dunne re-export voor
 * oude API-routes tot die weg zijn.
 */
export {
  logCallForLead,
  logContactForLead,
  statusForOutcome,
  statusForContact,
  type CallOutcome,
  type ContactOutcome,
  type ContactType,
} from "./contact-log";
